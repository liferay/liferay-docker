hosts:
    localhost:
        services:
            - antivirus
            - backup
            - ci
            - db
            - liferay
            - log-proxy
            - log-server
            - search
            - web-server